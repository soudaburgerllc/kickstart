# This file is managed by ansible.
conf-dir=/etc/dnsmasq.d,.rpmnew,.rpmsave,.rpmorig
listen-address={{ "," if not dnsmasq_listen_addresses.last }}
{{ for interface in dnsmasq_listen_interfaces }}
interface={{ interface }}
{{ endfor }}